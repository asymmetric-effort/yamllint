{
  "name": "@asymmetric-effort/yamllint-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "test:install": "npx playwright install --with-deps chromium",
    "test": "npx playwright test"
  },
  "dependencies": {
    "@asymmetric-effort/specifyjs": "latest"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^6.4.0",
    "@playwright/test": "^1.59.0"
  }
}
